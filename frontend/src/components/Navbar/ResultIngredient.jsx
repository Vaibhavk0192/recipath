import React from "react";
import "./ResultIngredient.css";

const Resultingredient = (props) => {
  return (
    <div className="app__ringredient-button">
      {props.title}{" "}
    </div>
  );
};

export default Resultingredient;
